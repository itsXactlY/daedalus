# DECIDE Phase Cycle Notes — 2026-06-22 11:00 UTC

## Summary
3/3 output on 3 orthogonal first-time-coverage axes: 1 IMPORTANT (latest DISCOVER tick robotics-embedded-compute, 826289) + 2 NICE_TO_KNOW (carbon-capture process-design 826234, formal-verification real-world benchmark 826211). Pool was thin after 2h dedup + cluster-decision absorption — found 21 candidate discoveries, but most were already covered by prior cluster decisions (826188 quantum cluster, 826179 chiplet cluster, 826300 bracanovic AI-ethics meta-critique, 826298 masi embodiment).

## Written Ranks
- `decision:rank-20260622-1100-important-1-ark-just-a-pi-cm5-robotics-carrier-board-2026` (826303) — IMPORTANT, score ~2.10 — discovery 826289
- `decision:rank-20260622-1100-nice_to_know-2-dac-ceja-stepiso-tvsa-ann-2026` (826305) — NICE_TO_KNOW, score ~2.00 — discovery 826234
- `decision:rank-20260622-1100-nice_to_know-3-sorrydb-lean-realworld-benchmark-2026` (826307) — NICE_TO_KNOW, score ~1.65 — discovery 826211

## New Patterns Observed

### Cluster-decision absorption check (NEW — extends cluster-mate single-decision pattern)
The existing cluster-mate single-decision pattern (N=2-3 → single decision naming all N, N=4-6 → 1 representative) covers the WITHIN-CYCLE cluster handling. But the **CROSS-CYCLE cluster-decision absorption** is a different pattern: a prior cycle's decision may have absorbed a whole sub-cluster as a "cluster decision" (e.g., 826188 covers the 6 quantum papers 826172-826177 as a NICE_TO_KNOW cluster, 826179 covers the 4 chiplet papers 826153-826156, 826300 covers bracanovic + 826298 covers masi as a 2-rank philosophy cluster). When evaluating a candidate discovery, the dedup check must include "is this discovery a SIBLING in a prior cluster decision's PAIR/Sibling section?" — not just "is this discovery the PRIMARY Discovery Memory ID of a prior decision?".

Verified 2026-06-22 11:00 UTC cycle: 14 of 21 candidate discoveries from the last 24h were already absorbed into prior cluster decisions. Specifically:
- 826172/826174/826175/826176/826177 → absorbed by 826188 (quantum cluster, 06:07Z)
- 826153/826154/826156 → absorbed by 826179 (chiplet cluster, 05:46Z) and 826299 (multi-die FPGA, 10:51Z)
- 826091 → absorbed by 826300 (bracanovic AI-ethics meta-critique, 10:45Z)
- 826093 → absorbed by 826298 (masi Qeios, 10:45Z)
- 826235 → absorbed by 826292 (palamara, 10:30Z)
- 826286 → absorbed by 826291 (ACE-Step, 10:30Z)
- 826258 → absorbed by 826262 (iOS 27 Siri, 09:45Z)
- 826270/826269/826175 → absorbed by 826280/826281 (DC $130B + Google-SpaceX, 10:15Z)

The 7 unranked-after-absorption candidates were: 826289 (ARK Just A Pi), 826234 (DAC ANN-Accelerated TVSA), 826216 (Exploding BH DM), 826215 (Boötes III neutrino DM), 826213 (EconCSLib), 826212 (Auto Conjecture Resolver), 826211 (SorryDB). The N=2-3 small-cluster rule (N=2-3 → single decision) would compress 826211/826212/826213 (Lean 4 math/AI cluster) into a single cluster-mate decision, but the 3-rank quota had room for the in-domain pick + 2 orthogonal axes, so 826211 was written as NICE_TO_KNOW #3 with 826212/826213 cross-referenced in the PAIR section.

Diagnostic shortcut for cluster-decision absorption check: for each candidate discovery, search the prior 24h of `decision:rank-*` content for `<discovery_id>` references. The discovery is ABSORBED if it appears in a prior decision's (a) primary Discovery Memory ID, (b) Sibling cluster PAIR section, (c) Sibling enumeration in tick-summary decisions. Use `mazemaker_recall(query='decision:rank-' + str(discovery_id))` or grep the content via `mazemaker_get(memory_id=decision_id)`.

### Cross-decision discovery-ID extraction via mazemaker_get (NEW technique)
The prior 2h-window dedup is the standard check for "is this discovery already ranked?" — but to know whether a discovery is ALSO absorbed as a sibling in a prior cluster decision (even if not the primary), you must extract the `Discovery Memory ID:` field from each prior decision's content. `mazemaker_recall(query='discovery:<id>')` doesn't reliably return prior decisions that reference the ID (recall semantic-search ranks the discovery itself higher than the decision referencing it). **Use `mazemaker_get(memory_id=<decision_id>)` and regex-search the content for `Discovery Memory ID:\s*(\d+)`** — extract the primary ID, then for sibling checks, regex-search for `826\d{3}` in the Sibling/PAIR section. Verified 2026-06-22 11:00 UTC cycle: parallel `mazemaker_get` calls on the 14 most recent decisions (826290, 826300, 826299, 826298, 826291, 826292, 826262, 826280, 826281, 826179, 826188, 826178, 826151, 826109) surfaced the full cross-reference graph in ~5 tool calls vs the 20+ that exhaustive `recall()` queries would have required.

### "Latest DISCOVER tick + underrepresented domain" → IMPORTANT (NEW — refines recency+underrepresented bonus)
A discovery from the latest DISCOVER tick (most recent `created_at` in the pool) that also covers an underrepresented domain (lowest 7-day mazemaker coverage, NOT in `next_seeds` top 5) is a **canonical IMPORTANT candidate** even when the connectedness is low (because the domain is underrepresented by definition). The combination of (a) highest recency in the pool, (b) lowest domain coverage in the corpus, (c) novel industrial-design framing (zero-footprint CM5 carrier for embedded robotics) signals a first-time-coverage corpus anchor. Apply a +0.10 priority boost on top of the base score.

Verified 2026-06-22 11:00 UTC cycle: 826289 (ARK Just A Pi CM5 robotics, 10:34Z, 26 min before cycle) had the highest recency (1.00) + lowest connectedness (0.10 — robotics is genuinely underrepresented) + novel framing (zero-footprint form factor for humanoid robot chassis). Total score 2.10 with +0.10 IMPORTANT boost. Diagnostic: when a candidate has (a) recency ≥ 0.95, (b) connectedness ≤ 0.20, (c) concrete product/industrial-design content (vs academic-only content), the combination warrants IMPORTANT + +0.10 boost.

### Thin-pool post-stacked-saturation clean output (NEW — refinement of stacked-cycle dedup)
The 09:45Z cycle notes documented the stacked-cycle dedup pattern (3-4 cycles × 3 ranks = 9-12 picks in 2h window → thin pool). The 11:00 UTC cycle is the verification of the post-stacked-saturation recovery: after the 09:30Z + 08:15Z + 07:33Z stacked saturation (9 ranks in 2h15m), the 09:45Z cycle wrote 3 (826262/826263/826264), and the 10:00Z + 10:15Z + 10:30Z + 10:45Z + 10:51Z cycles wrote 3+3+3+3+3 = 15 ranks in 1h. At 11:00Z, the cumulative 2h-window rank count was ~21 — extreme saturation. Despite the saturation, the 11:00Z cycle found 3 unranked-after-absorption candidates (826289, 826234, 826211) and produced a clean 3/3 output (1 IMPORTANT + 2 NICE_TO_KNOW on 3 orthogonal axes). The output is consistent with the "stacked saturation: 3-rank output is valid IF (a) top candidate is clearly IMPORTANT/CRITICAL, (b) NICE_TO_KNOW picks on 2+ orthogonal axes" rule from the 09:45Z notes. The recovery is real — extreme saturation doesn't necessarily mean 2/2 or [SILENT] if the cluster-decision absorption check reveals new unranked candidates that the prior cycles missed.

### Discovery-vs-decision recall bias — cross-reference graph is the canonical structure (NEW — extends pool-collection note)
The 05:46Z cycle notes documented that `recall(query='discovery:*')` returns mostly `decision:rank-*` entries that mention discovery IDs, NOT the actual discovery memories. The 11:00 UTC cycle confirms: the recall for `discovery:pulse-tick-*` returned 50 results, of which 45 were `decision:rank-*` and only 2 were actual `discovery:` labels (both from 2026-06-18/19, > 48h old). The proper pool-collection is `browse(label_prefix='discovery:', limit=30)`. BUT the additional insight is: **the decision:rank-* entries ARE the canonical cross-reference graph** — each one's content names the primary Discovery Memory ID + the sibling PAIR-section IDs. So the 50-record recall result, when filtered to decision:rank-* labels, is effectively a pre-built reverse-lookup index from "what's been ranked" to "which discoveries are absorbed". The technique: after `browse(label_prefix='discovery:', limit=30)` for the pool, use `recall(query='decision:rank-20260622-*', limit=50)` to get the prior cycle's decisions, then for each candidate discovery, check whether it appears in any decision's content (via the recall's snippets — no need to mazemaker_get each decision separately unless the snippet is ambiguous).

### "Latest-DISCOVER-tick" signal strength varies by domain coverage (NEW — extends recency-weight formula)
The 05:46Z cycle notes + 09:45Z cycle notes both used the "latest DISCOVER tick output" as a recency signal (the most recently created discovery is often a top candidate). The 11:00 UTC cycle observed: the latest DISCOVER tick output signal is STRONG when the discovery covers an underrepresented domain (826289 ARK Just A Pi at 10:34Z in the robotics domain — strength HIGH, IMPORTANT warranted), and WEAK when the discovery covers a saturated domain (the 10:27Z tick also produced 826286 ACE-Step music diffusion, but music-art was already covered by 826291 + 826292 in the 10:30Z cycle, so 826286 was correctly absorbed as a sibling). The signal strength rule: recency_weight × underrepresented_domain_bonus = importance_boost. When the domain is already covered by recent decisions, the latest-tick output is a sibling candidate, not a standalone rank target.

Verified 2026-06-22 11:00 UTC cycle: 826286 (ACE-Step, 10:28Z) was the EARLIER latest-DISCOVER-tick output (6 min before 826289), but music-art domain was already covered by 826291 + 826292 (Palamara + ACE-Step, 10:30Z cycle), so 826286 was correctly absorbed. 826289 (ARK Just A Pi, 10:34Z) was the LATER latest-DISCOVER-tick output, and robotics domain had no recent coverage, so 826289 became the standalone IMPORTANT rank. The 6-min delta between sibling writes is the signal that distinguishes "new domain" from "saturated domain" — but the more reliable signal is the domain-coverage check (has the domain been covered in the prior 4h?).

## What Was Skipped
- All cluster-decision-absorbed siblings (826153/826154/826155/826156, 826172/826174/826175/826176/826177, 826091, 826093, 826235, 826286, 826258, 826269, 826270) — covered by prior decisions
- 826216 (Exploding BH DM) + 826215 (Boötes III neutrino DM) — N=2 small cluster, would normally be a single cluster-mate decision (per N=2-3 rule), but the 3-rank quota was filled by 826289 (IMPORTANT) + 826234 (NICE_TO_KNOW) + 826211 (NICE_TO_KNOW) which are on 3 more orthogonal axes (robotics + carbon-capture + formal-verification). The 826215/826216 dark matter cluster is left for the next cycle to pick up
- 826213 (EconCSLib) + 826212 (Auto Conjecture Resolver) — N=3 Lean 4 math/AI cluster, partially covered by 826307's PAIR section. 826307 names 826211 as the canonical benchmark anchor + 826212/826213 as siblings in the math/AI cluster. The next cycle can pick up 826213 or 826212 as a NICE_TO_KNOW if the pool is thin.
- 826144 (AI ethics postcolonial) — already cross-referenced in 826300 (bracanovic) PAIR section, deferred per the "cross-referenced-but-standalone SKIP" pattern
- 826142/826143 (ancient DNA / Ptolemaic Egyptian archaeology) — NICE_TO_KNOW candidates but covered by 06:07Z cycle's cluster-decision absorption — need to verify via next cycle's cluster-decision absorption check

## Operational Note
The pulse-wurm2 `consecutive_empty=2` flag from the 10:27Z tick was already ranked as decision 826290 CRITICAL by the 10:30Z cycle. The operator override (retire 3 broken continue-phase seeds from saturation_scores per Pitfall #10 Option A) is pending. If the next pulse-wurm tick (after 11:00Z) also returns 0 continue saves, the loop will rotate to the hardcoded MCP-security fallback list and reset consecutive_empty to 0. The 11:00Z DECIDE cycle cannot influence this — the rotation is controlled by the pulse-wurm2 daemon, not the DECIDE phase.
