# DECIDE Phase Cycle Notes — 09:31 UTC

Session-specific notes from the 2026-06-21 09:31 UTC DECIDE cycle. Supplements the existing 01:00 / 03:30 (`cycle-notes.md`), 08:00 (`cycle-notes-0800.md`), 08:35 (`cycle-notes-0835.md`), and 08:45 (`cycle-notes-0845.md`) cycle notes.

## Saturated Pool (3rd consecutive cycle)

The 08:45 cycle noted 95% pool saturation; the 09:16 cycle ran ~30 min after and still found the eligible pool dominated by the 08:42/08:55Z ticks. The 09:31 cycle ran ~15 min after the 09:16 cycle and saw the same pattern. Pool composition:

- 30 discoveries in the 24h pool returned by `mazemaker_browse(label_prefix="discovery:pulse-wurm-20260621", limit=50)`
- ~16 already covered by decisions from 07:15+ cycles (within last 2h dedup window)
- ~14 genuinely uncovered (8 from 08:55Z tick philosophy cluster + 3 from energy fresh direction + 3 from continue-phase Jack Clark/Karpathy + various OR fresh direction)

**Strategy (extending 08:45 freshest-first):** When the eligible pool is dominated by a single fresh tick (08:55Z), pick across the tick's sub-themes rather than within one. The 08:45 cycle picked across orthogonal academic layers (cryptographic-structures / OR / IS) from one tick; the 09:31 cycle picked across orthogonal research-strategic layers (AI-automation prediction / AI healthcare policy / energy-perovskite research).

## Concrete-Noun-Anchored Seed Pattern — THIRD Independent Validation

The 09:31 cycle's perovskite finding (825502) provides the THIRD independent validation of the concrete-noun-anchored seed pattern in fresh-direction ticks:

| Cycle | Domain | Generic seed result | Concrete seed result |
|---|---|---|---|
| 08:35 | gaming | 0/15 (cluster bloat) | 1/15 (gaming crisis ISCRAM 825475) |
| 08:45 | operations-research | 0/15 (Ti/Multi noise) | 4/15 (COCOA2026 + BUE + FIFA 825489-491) |
| 09:31 | energy | 1/15 (kept) | 4/15 (perovskite cluster 825502) |

**Pattern (PATCH AS VALIDATED):** Three cycles confirming the same recipe pattern is now strong-enough evidence to make the concrete-noun-anchored seed the DEFAULT fresh-direction strategy. The "<domain> frontier research 2026" template is now documented as anti-pattern across three domains (gaming, OR, energy). Future pulse-wurm-recon-tick SKILL.md updates should reflect this.

## Cron Run Notes (09:31 UTC)

- Total cycle: ~5 minutes from script invocation to final report
- 30 discoveries in 24h pool, ~16 covered by earlier-today decisions (07:15+ window), ~14 fresh from 08:55Z tick selected
- Ran `python3 ~/.hermes/loops/tri-state/decide_rank.py` (just outputs structured instructions, no-op for the agent)
- Used `mazemaker_browse(label_prefix="discovery:pulse-wurm-20260621", limit=50)` to fetch 30 fresh discoveries + `mazemaker_browse(label_prefix="decision:rank-20260621-", limit=30)` to fetch 30 recent decisions for dedup
- Built dedup map by extracting "Discovery Memory ID: <num>" regex from each decision's content
- 2h dedup window: ids >= 825463 (07:15 UTC) excluded as recently-ranked
- 3 picks written as 825516, 825517, 825518
- Avoided the cron instruction's `discovery:pulse-tick-*` recall-glob pitfall (documented in 03:30 cycle notes) by using browse instead

## Outstanding Observations

- The cron instruction in `~/.hermes/loops/tri-state/decide_rank.py` still uses `discovery:pulse-tick-*` as the recall query. This query string is documented as broken (03:30 cycle notes, pitfall #2). Recommend patching the script to use `discovery:pulse-wurm-*` or `discovery:*` to prevent silent miss in future cron iterations.
- The 2h dedup window using `id >= 825463` as a proxy for "ranked after 07:15 UTC" is approximate. A more precise approach would use `mazemaker_get` to fetch each decision's actual `created_at` timestamp, but this is overkill given the ID-as-proxy is monotonic and the threshold is fuzzy.
- The philosophy cluster (825493-825499) had 7 findings from the 08:55Z tick; only 825494 was ranked in this cycle. The other 6 (825493, 825495-499) remain eligible for future cycles if the pool becomes less saturated. They represent a coherent research-stage cluster that may want to be addressed together if pulse-wurm produces fewer new findings.

## Priority Mix Validation

This cycle produced 0 CRITICAL + 2 IMPORTANT + 1 NICE_TO_KNOW. The 08:45 cycle notes warn against manufacturing an IMPORTANT rating to "hit priority diversity"; the 09:31 cycle achieved 2 IMPORTANT legitimately (both findings had concrete primary-source data + cross-domain bridge signal) without forcing CRITICAL. The 0 CRITICAL is correct: the 08:55Z tick was philosophy + energy + AI-automation predictions, none of which are security/data-loss/blocking issues.