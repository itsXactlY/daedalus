### Run #28 — 2026-06-23 — maze-crew-trailer.html
**Change:** Boot fade-in — cinematic reveal from black, symmetric to the existing `#fade` loop-seam fade-out. Three coordinated changes: (1) CSS extends `#c`'s `transition:` shorthand from `filter 0.7s ease` to `filter 0.7s ease, opacity 0.8s ease` and adds initial state `opacity: 0`; (2) JS in init() writes `_canvasEl.style.opacity = '1'` AFTER the `_canvasEl = document.getElementById('c')` resolution line; (3) the 0.8s canvas fade is intentionally 200ms longer than the 0.6s `#loading` fade-out so the brain emerges from the void rather than arriving pre-lit. Trailer-only — bio and dream don't have loading overlays.

**TDZ/ordering trap caught mid-run (NEW pitfall 1d in SKILL.md).** Initial placement put the opacity write right next to `loading.classList.add('gone')` at the Ready block (L617), BEFORE `_canvasEl = document.getElementById('c')` was resolved at L653. The defensive `if (_canvasEl)` guard correctly returned false (canvas ref still null) and silently swallowed the operation — the canvas would have stayed at `opacity: 0` forever, producing a permanently black screen after the loading text faded. No runtime error, no `node --check` failure, no console output. Fixed by moving the new line to immediately after `_canvasEl = document.getElementById('c')` at L668. **Detection recipe:** before committing any patch that adds a new use of an existing cached DOM ref, verify use site line > resolve site line.

**Synchronous init() CSS-transition batching (also captured in pitfall 1d).** Once ordering is fixed, will the two CSS transitions start at the same moment? Yes — init() runs SYNCHRONOUSLY top-to-bottom. Both DOM mutations (`loading.classList.add('gone')` at L617 + `_canvasEl.style.opacity = '1'` at L668, source-order distance 51 lines) are committed during the same synchronous JS execution cycle. The browser batches the style invalidations and starts BOTH CSS transitions on the next paint. If init() were async, the cross-fade would become a sequential fade.

**Why extend `transition:` rather than replace it.** `#c`'s existing `filter 0.7s ease` is used by `startFadeLoop` for chromatic desaturation on loop seams. Adding `, opacity 0.8s ease` appends a new property — both transitions tracked independently, run in parallel, never conflict. CSS transitions are per-property, so loop-seam filter transition (every 20s) and boot opacity transition (once) coexist without interference.

**Magnitude choices.** 0.6s loading vs 0.8s canvas = 200ms mismatch — at t=0.6s loading is fully gone but canvas is at 75% opacity; brain visibly emerges from black. `opacity: 0` initial state: needed so CSS transition has something to interpolate FROM. No `display: none`: keeping canvas in layout (transparent) means WebGL renderer runs from t=0 — by the time opacity reaches 1.0, the maze is already mid-animation.

**Cross-file parity state:** trailer-only feature. No cross-file parity gap created or closed. Trailer heartbeat channel count remains 7 (boot fade is one-shot, not per-frame).

**Deferred-fix queue remaining in trailer:** NONE. All matrix rows ✓. Trailer is at full feature density — every priority-list item from the system prompt is implemented, plus the boot fade-in polish.

**ROTATION:** bio (#26) → dream (#27) → trailer (#28 = THIS RUN, ✓) → bio ← NEXT.

---

### Run #29 — 2026-06-23 — maze-crew-bio.html
**Change:** Cognitive thought-sparks — bio's first density-filling addition. Additive point particles emitted from the consciousness core on each heartbeat peak, flying outward 1.8-4.5 u/s into the cosmos over a 1.5s lifetime with phase-tinted hue. SPARK_CAP=40 round-robin pool, all parked at sentinel y=-9999 at boot. Same construction pattern as firingRings (FIRING_RING_CAP=120): BufferGeometry + 2 BufferAttributes (DynamicDrawUsage for per-frame uploads) + PointsMaterial with size 0.18, vertexColors, AdditiveBlending, no depth-write, sizeAttenuation. Birth hue locked to `currentBeatHue` (module-scope, set by `lerpPhaseColors()`). Spark position jitter `(rand-0.5)*0.3` on each axis. Velocity uses `Math.acos(2r-1)` for correct area-weighted phi so sparks don't cluster at the poles. Per-spark bell-curve brightness via `Math.sin(life * Math.PI)`, vertex color magnitude 1.5× for additive headroom. **Per-phase spark rate sanity:** NREM 0.25 sparks/sec (1 quiet drift), AWAKE/INSIGHT 1 spark/sec (steady heartbeat), REM 1.6 sparks/sec (rapid chaotic release — bursty). Average lifetime overlap at peak REM ~2.4 sparks visible. **Spark emission check:** `Math.pow(Math.max(0, Math.sin(beatPhase)), 6) > 0.6 && (clock.getElapsedTime() - _lastSparkSpawn) > 0.35`. **Cross-file parity state:** matrix row "Cognitive thought-sparks" goes from ⬜ across all three to bio ✓ #29 (only). Trailer + dream still ⬜. **Deferred-fix queue remaining in bio:** NONE. Bio is at full feature density — every priority-list item implemented plus several extras.

**ROTATION:** trailer (#28) → bio (#29 = THIS RUN, ✓) → dream ← NEXT.

---

### Run #30 — 2026-06-23 — maze-crew-dream.html
**Change:** Cognitive thought-sparks — dream backport, closes the cross-file parity gap with bio #29. Same construction pattern: BufferGeometry + DynamicDrawUsage + PointsMaterial size 0.18, vertexColors, AdditiveBlending, no depth-write. 40-slot round-robin pool, all parked at sentinel y=-9999 at boot. Spawn on each heartbeat peak with the same `^6` envelope + 0.35s debounce as bio. Birth hue locked to `cur.hue` (the per-phase hue lerped by `lerpState()` each frame and stored on the module-scope `cur` object). **Y-origin tracking** (the only file-specific delta): sparks emit from `(rand-0.5)*0.4, brainShell.position.y + (rand-0.5)*0.4, (rand-0.5)*0.4` instead of fixed origin. The dream's brain drifts vertically with phase (cur.bgY ranges from -2.7 NREM to +2.6 INSIGHT, scaled by *2.5 → -6.75 to +6.5 world units). Without y-tracking, REM sparks would bloom from y=-1 while the brain sits at y=-6.75 — visually disconnected. With y-tracking, sparks always emit from the sleeping mind's current vertical position. **Per-phase rate sanity:** NREM 0.25/sec, AWAKE/INSIGHT 1/sec, REM 1.6/sec (matches bio). Average lifetime overlap ~2.4 sparks at peak REM. **Module-scope state added:** SPARK_CAP=40, SPARK_LIFE=1.5, SPARK_SPEED_MIN/MAX=1.8/4.5, _sparkE (hoisted Color), sparkMesh, _sparkPos/Vel/Life/Hue, _sparkNextIdx, _lastSparkSpawn. **buildSparks()** called in init() after `buildFiringRingMesh()`. **updateSparks(dt)** called in animate() after `updateFiringRings(dt)`. **Cross-file parity state:** matrix row "Cognitive thought-sparks" goes from bio ✓ #29 + dream ⬜ to bio ✓ #29 + dream ✓ #30. Trailer still ⬜. **Deferred-fix queue remaining in dream:** NONE — every matrix row ✓.

**ROTATION:** bio (#29) → dream (#30 = THIS RUN, ✓) → trailer ← NEXT.

---

### Run #31 — 2026-06-23 — maze-crew-trailer.html
**Change:** Cognitive thought-sparks — trailer backport, closes the cross-file parity gap with bio #29 + dream #30. Same construction pattern: BufferGeometry + DynamicDrawUsage + PointsMaterial size 0.18, vertexColors, AdditiveBlending, no depth-write. 40-slot round-robin pool, all parked at sentinel y=-9999 at boot. Spawn on each heartbeat peak with the same `^6` envelope + 0.35s debounce as bio/dream. Birth hue passed as parameter (see birth-hue adaptation below). **Birth-hue adaptation** (the file-specific delta #1, captured in SKILL.md Specific Techniques "Cognitive thought-sparks" section): trailer's `hue` is a `const` declared INSIDE `animate()` at the phase-fog block, NOT module-scope like bio's `currentBeatHue` or dream's `cur.hue`. If `updateSparks()` declared at module scope references `hue` directly, it throws `ReferenceError` on first animate frame (free variables resolve through lexical declaration scope, not call scope). Fix: `updateSparks(dt, birthHue)` called as `updateSparks(dt, hue)` from animate. Mirrors trailer's inter-region thread bridge pattern (`t.dstHue` carried in the data record). **No y-origin tracking** (file-specific delta #2): trailer's nucleus stays at fixed (0,0,0) — no brainShell drift like dream. Symmetric jitter on all three axes. **Origin-spread tuning** (file-specific delta #3): trailer uses 0.4u spread vs bio/dream's 0.3u. Rationale: trailer's heart rate is slower (rph 0.5-0.8 Hz from `0.5 + sin(elapsed * 2π/LOOP_DURATION) * 0.3`), so sparks fire less frequently. The larger spread ensures simultaneous sparks at trailer's peak rate still visibly separate as distinct particles rather than overlapping. **Per-phase spark rate:** rph range 0.5-0.8 Hz → 0.5-0.8 sparks/sec; 0.35s debounce allows exactly 1 fire per beat; ~1.2 sparks visible at peak rate; pool saturation ~50s (far beyond 1.5s lifetime — round-robin never loses a spark). **Cross-file parity state:** matrix row "Cognitive thought-sparks" goes from bio ✓ #29 + dream ✓ #30 + trailer ⬜ to bio ✓ #29 + dream ✓ #30 + trailer ✓ #31 — full 3/3 parity at last. Trailer heartbeat channel count 7 → 8. **TDZ/lookup audit:** TOTAL_TIME (module-scope at L183) declared before updateSparks reads it via outer-scope closure; `hue` is passed as parameter (no TDZ risk); scene creation at L787 < buildSparks() call at L627 (init synchronous top-to-bottom). **Deferred-fix queue remaining in trailer:** NONE — every matrix row ✓, every priority-list item implemented plus boot fade-in + recognition ping composition. Trailer is at full feature density.

**ROTATION:** bio (#29) → dream (#30) → trailer (#31 = THIS RUN, ✓) → bio ← NEXT.

---