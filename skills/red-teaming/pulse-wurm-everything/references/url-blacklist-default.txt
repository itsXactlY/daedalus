# Default URL blacklist for pulse_search / pulse_dig output filtering
# Applied BEFORE quality scoring. One substring per line; URL matches if it contains any line.
# v2 updated 2026-06-18 10:21 tick — see pulse-dig-vs-search-structures.md for context.
# v3 updated 2026-06-18 23:00 tick — added current-year PR/issue number collisions (/pull/2026, /issues/2026).
#     These are false-positive matches where the search engine treats the literal year in a seed
#     query as a PR/issue number and returns random unrelated PRs. Year-dependent — needs
#     annual update (replace `2026` with the new year in January). See SKILL.md pitfall
#     "GitHub PR `/pull/2026` number-collision noise (2026-06-18 23:00)".
#     For a regex-based filter (parser-side), see the Python pattern at the bottom of this file.

# === GitHub marketing/landing pages (v2 hardened) ===
# The 09:15 v1 blacklist missed several patterns that the worm follows from any github.com URL.
# All v2 additions were observed in 10:21 tick dig output from security-audit-related seeds.
github.com/features
github.com/enterprise
github.com/pricing
github.com/security
github.com/topics
github.com/customer-stories
github.com/about
github.com/sponsors
github.com/readme
github.com/issues/
github.com/solutions/        # v2 — solutions/industry/healthcare, solutions/use-case/app-modernization
github.com/apps             # v2 — GitHub Apps marketplace landing
github.com/orgs             # v2 — org dashboard
/github/                    # v2 — catch-all for /github/ path segments (community nav, etc.)
github.com                  # v2 — root github.com with short path only (override in code: only block if path-slash-count <= 4)

# === GitHub subdomains and product landing pages (v2 additions) ===
docs.github.com
docs-internal.github.com    # v2 — internal docs (observed in 10:21)
github.community            # v2 — community forums
support.github.com          # v2 — support pages
services.github.com         # v2 — GitHub Services / Expert Services
resources.github.com        # v2 — GitHub Resources / blogs
github.com/site-policy
github.com/get-started
archiveprogram.github.com
desktop.github.com
cli.github.com
skills.github.com
securitylab.github.com

# === Bluesky profiles ===
bsky.app/profile

# === Reddit app / corporate pages ===
play.google.com/store/apps/details?id=com.reddit.frontpage
redditblog.com
www.redditinc.com
www.redditinc.com/advertising

# === W3C / standards ===
www.w3.org/1999/xhtml

# === Generic low-yield hostnames (optional, uncomment to enable) ===
# twitter.com/i/                # twitter internal redirects
# youtube.com/feed/             # youtube feed pages
# linkedin.com/company/         # linkedin company nav

# === Current-year PR/issue number collisions (v3, 2026-06-18 23:00) ===
# When a seed contains the literal year string, pulse_search returns random
# GitHub PRs/issues that happen to have that year as their PR/issue number.
# These are completely off-topic but score high on freshness (100) and local_relevance (0.20).
# Year-dependent — needs annual update in January.
# Substring form (matches anywhere in URL):
/pull/2026
/issues/2026

# Regex form for the parser (use re.search, not substring match):
# import re, datetime
# CURRENT_YEAR = datetime.now().year
# PR_COLLISION_RE = re.compile(rf"github\.com/[^/]+/[^/]+/(pull|issues)/{CURRENT_YEAR}(?:[/?#]|$)")
# if PR_COLLISION_RE.search(url): drop
