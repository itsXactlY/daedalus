# Built artifacts / caches
*.bak.rej
*.orig
*.rej
.cache/
__pycache__/
*.pyc

# Sensitive paths — never commit
**/id_rsa
**/id_ed25519
**/.ssh/
**/.gnupg/
**/.config/gh/hosts.yml
**/.netrc

# Session / runtime artifacts
*.pid
*.sock
*.log
*.tmp

# Massive directories (excluded — listed separately if wanted)
nohup.out